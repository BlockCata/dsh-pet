## Agent skills

### Issue tracker

建立、讀取或更新議題時，依 `docs/agents/issue-tracker.md` 使用本機 Markdown。

### Triage labels

分類議題時，使用 `docs/agents/triage-labels.md` 的五個預設標籤。

### Domain docs

採 single-context。探索程式碼前，依 `docs/agents/domain.md` 讀取相關領域文件。
